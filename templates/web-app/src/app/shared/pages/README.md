# Shared Pages

Shared pages are full-page components that are not part of a specific feature.

Examples:

- 404 Not Found page
- Error page
- Maintenance page

These pages are typically loaded via the catch-all wildcard route in `app.routes.ts`.
